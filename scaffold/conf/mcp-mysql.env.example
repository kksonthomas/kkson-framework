# Optional: copy to mcp-mysql.env for local MCP MySQL tooling.
# Application DB credentials still come from db.config.{env}.ini.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=your_user
MYSQL_PASS=your_password
MYSQL_DB=your_app
